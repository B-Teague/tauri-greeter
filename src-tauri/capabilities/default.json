{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The greeter window. The UI calls this crate's own commands plus event listening, which is how LightDM's PAM prompts reach it; no other core plugin API is granted.",
  "windows": ["main"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
